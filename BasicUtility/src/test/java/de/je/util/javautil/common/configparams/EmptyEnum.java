package de.je.util.javautil.common.configparams;

/**
 * {@link EmptyEnum} is an enum class that needs to be specified for {@link AbstractConfigurableTest} class instances if
 * there is no (further) enum parameter class to test.
 */
public enum EmptyEnum {
   // Intentionally empty
}
